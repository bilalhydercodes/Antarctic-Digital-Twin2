{
  "name": "antarctic-digital-twin",
  "version": "1.0.0",
  "private": true,
  "description": "Antarctic Digital Twin & Station Simulation Platform for Maitri and Bharati (MoES / NCPOR)",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "npm run build:server",
    "start": "cd server && npm start",
    "build:client": "cd client && npm install && npm run build",
    "build:server": "cd server && npm install && npm run build",
    "build:all": "cd server && npm install && npm run build && cd ../client && npm install && npm run build",
    "test:db": "node server/test-db.cjs"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
